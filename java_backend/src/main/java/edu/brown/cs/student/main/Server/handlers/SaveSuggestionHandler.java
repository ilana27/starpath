package edu.brown.cs.student.main.Server.handlers;

/**
 * Handler for saving a suggestion to the backend
 */
public class SaveSuggestionHandler {

  /**
   * what to save for suggestions handler:
   * - the string of the actual suggestion (e.g. 'go for a walk')
   * - frequency times clicked
   * -
   * - associated user emotion
   *
   * what to save as inputs for algorithm:
   * -
   * -
   */
    
}
